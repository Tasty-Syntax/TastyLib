{
  "name": "tasty-lib",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && node scripts/bundle.ts && npm run build:declaration && npm run build:strip-comments && rm -rf dist/bundle.ts",
    "build:bundle": "node scripts/bundle.ts",
    "build:declaration": "tsc --project tsconfig.declarations.json",
    "build:strip-comments": "node scripts/strip-docs.ts"
  },
  "keywords": [
    "Battlefield 6",
    "BF 6",
    "Battlefield Portal"
  ],
  "author": "Tasty Syntax",
  "license": "LGPL-3.0-or-later",
  "devDependencies": {
    "ts-morph": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
